name: Django CI (pip)

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: damodara
          POSTGRES_PASSWORD: damodara
          POSTGRES_DB: diary
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.13'

    - name: Cache pip dependencies
      uses: actions/cache@v4
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pytest pytest-django pytest-cov

    - name: Run migrations
      env:
        DB_NAME: diary
        DB_USER: damodara
        DB_PASSWORD: damodara
        DB_HOST: localhost
        DB_PORT: 5432
        DJANGO_SETTINGS_MODULE: config.settings
      run: |
        python manage.py migrate

    - name: Run pytest with coverage
      env:
        DB_NAME: diary
        DB_USER: damodara      # исправлено
        DB_PASSWORD: damodara  # исправлено
        DB_HOST: localhost
        DB_PORT: 5432
        DJANGO_SETTINGS_MODULE: config.settings
      run: |
        pytest --cov=. --cov-report=xml